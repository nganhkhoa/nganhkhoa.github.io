{
  "name": "elm-pages-app",
  "type": "module",
  "scripts": {
    "postinstall": "elm-tooling install",
    "start": "elm-pages dev --port 8080",
    "build": "elm-pages build",
    "publish": "scp -r dist/* nganhkhoa@nganhkhoa.com:~/website/"
  },
  "devDependencies": {
    "elm-codegen": "^0.3.0",
    "elm-optimize-level-2": "^0.1.2",
    "elm-pages": "^3.0.3",
    "elm-review": "^2.10.2",
    "elm-tooling": "^1.14.0",
    "vite": "^4.3.5"
  },
  "dependencies": {
    "@netlify/functions": "^1.4.0",
    "elm-format": "^0.8.7"
  }
}
